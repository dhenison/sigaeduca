import {useState} from 'react';
import {Icon} from '../components/UI';
import {authService} from '../services/auth';
export default function Login({onDemo}:{onDemo:()=>void}){const [show,setShow]=useState(false),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false),[reset,setReset]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{if(reset){await authService.reset(email);setError('Confira seu e-mail para continuar.')}else await authService.signIn(email,password)}catch(e){setError(e instanceof Error?e.message:'Não foi possível entrar.')}finally{setBusy(false)}}return <main className="login"><div className="login-hero"><p>EDUCAÇÃO<br/>DISCIPLINA<br/>OPORTUNIDADES<br/>GRANDES FUTUROS</p></div><div className="login-body"><div className="login-brand"><img src="/icon.svg" alt=""/><h1>SIGA <b>EDUCA</b></h1><strong>GESTÃO ESCOLAR PARA GRANDES CONQUISTAS</strong></div>{reset&&<h2>Recupere seu acesso</h2>}<form onSubmit={submit}><label className="field"><Icon name="mail"/><input aria-label="Seu e-mail institucional" type="email" required placeholder="Seu e-mail institucional" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="username"/></label>{!reset&&<label className="field"><Icon name="lock"/><input aria-label="Sua senha" required type={show?'text':'password'} placeholder="Sua senha" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/><button type="button" aria-label={show?'Ocultar senha':'Mostrar senha'} onClick={()=>setShow(!show)}><Icon name={show?'eyeOff':'eye'}/></button></label>}{error&&<div role="status" className="info">{error}</div>}<button className="primary" disabled={busy}>{busy?'Aguarde…':reset?'Enviar instruções':'Entrar'}<Icon name="arrow"/></button></form><button className="text-button recovery" onClick={()=>{setReset(!reset);setError('')}}>{reset?'Voltar ao login':'Esqueceu sua senha?'}</button><div className="divider"><span/>ou<span/></div><button className="outline" onClick={()=>setError('O acesso SEDUC estará disponível após a integração institucional.')}><Icon name="school"/>Entrar com conta SEDUC<Icon name="next"/></button><p className="motto">“Educação transforma<br/>realidades.”</p><button className="demo-button" onClick={onDemo}>Explorar demonstração <Icon name="arrow"/></button></div><footer className="login-footer"><span className="para-flag">★</span><div><small>Governo do</small><b>PARÁ</b></div><span className="footer-line"/><div><b>SEDUC</b><small>SECRETARIA DE ESTADO<br/>DE EDUCAÇÃO</small></div></footer></main>}
